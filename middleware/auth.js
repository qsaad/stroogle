export default({ store, route, redirect }) => {
    console.log('Check User Login')
    // if(!store.getters.isAuthenticated && route.name !== 'login'){
    //   redirect('/login')
    // }
    // else{
    //   console.log('Welcome registered user ' + store.getters.getEmail)
    // }
}